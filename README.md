Homework 2

The project can be executed using the commands:
- make all, to compile all the necessary files 
- make run, to execute the generated code
- make clean, to delete the generated files 

Before executing the make run command in the Makefile, the files that will be given as input to the program must be 
placed in the "inputfiles" position.